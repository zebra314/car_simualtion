<launch>

    <!-- Init pose -->
    <arg name="x" default="0"/>
    <arg name="y" default="3"/>
    <arg name="z" default="0.23"/>
    <arg name="yaw" default="1.5708"/>
    <arg name="rviz" default="true"/>
    
    <!-- Rviz -->
    <node name="rviz" pkg="rviz" type="rviz" args="-d $(find pollo_navigation)/rviz/amcl.rviz" 
      if="$(arg rviz)"/>
      
    <!-- Robot -->
    <include file="$(find pollo_gazebo)/launch/spawn.launch" >
      <arg name="x" value="$(arg x)" />
      <arg name="y" value="$(arg y)" />
      <arg name="z" value="$(arg z)" />
      <arg name="yaw" value="$(arg yaw)" />
    </include>

    <!-- AMCL -->
    <include file="$(find pollo_navigation)/launch/include/amcl.launch" >
      <arg name="initial_pose_x" value="$(arg x)" />
      <arg name="initial_pose_y" value="$(arg y)" />
      <arg name="initial_pose_a" value="$(arg yaw)" />
    </include>
  
</launch>